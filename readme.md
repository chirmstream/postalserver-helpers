These bash scripts contain the default docker-run command to start the required mariadb and caddy containers for postal server.

See [Postal docs](https://docs.postalserver.io/getting-started/prerequisites)